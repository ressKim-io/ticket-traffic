# DEPRECATED: Use ExternalSecret (infra/k8s/external-secrets/jwt-external-secret.yaml) instead.
# This file is retained only for local development and testing.
apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  namespace: sportstix
  labels:
    app.kubernetes.io/part-of: sportstix
type: Opaque
stringData:
  JWT_PRIVATE_KEY: "CHANGE_ME_TO_RSA_PRIVATE_KEY_PEM"
  JWT_PUBLIC_KEY: "CHANGE_ME_TO_RSA_PUBLIC_KEY_PEM"
  JWT_KID: "sportstix-1"
