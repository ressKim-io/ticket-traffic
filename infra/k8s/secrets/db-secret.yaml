# DEPRECATED: Use ExternalSecret (infra/k8s/external-secrets/db-external-secret.yaml) instead.
# This file is retained only for local development and testing.
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: sportstix
  labels:
    app.kubernetes.io/part-of: sportstix
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: "CHANGE_ME"
  POSTGRES_PASSWORD: "CHANGE_ME"
