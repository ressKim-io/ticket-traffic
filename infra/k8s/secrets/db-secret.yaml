apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: sportstix
  labels:
    app.kubernetes.io/part-of: sportstix
type: Opaque
# Values below are placeholders - replace with real secrets via sealed-secrets or external-secrets
stringData:
  SPRING_DATASOURCE_PASSWORD: "CHANGE_ME"
  POSTGRES_PASSWORD: "CHANGE_ME"
